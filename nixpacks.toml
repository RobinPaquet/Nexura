[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.virtualenv", "postgresql"]

[phases.install]
cmds = ["python3.11 -m virtualenv .venv", ".venv/bin/pip install -r requirements.txt"]

[start]
cmd = ".venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
